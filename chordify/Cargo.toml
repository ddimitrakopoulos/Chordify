[package]
name = "chordify"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] } # Asynchronous runtime for TCP client-server communication

sha1 = "0.10"
hex = "0.4" 

serde = { version = "1.0", features = ["derive"] } # Serialization/deserialization library
serde_json = "1.0" 

rustyline = "13.0"